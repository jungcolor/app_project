import React from 'react';

const TodoAppend = () => {
    return (
        <div className="todo-append">
            <input type="text" />
            <button type="button">+</button>
        </div>
    );
};

export default TodoAppend;