import React from "react";
import Navigation from "../_molecules/Navigation";

const Header = () => {
    return (
        <header>
            <Navigation />
        </header>
    );
};

export default Header;
