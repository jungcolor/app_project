import React from "react";
import ArticleItem from "../_atoms/ArticleItem";

const ArticleList = () => {
    return (
        <ul>
            <ArticleItem />
        </ul>
    );
};

export default ArticleList;
