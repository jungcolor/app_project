import React from 'react';
import { Container, Header } from '@pages/Channel/styles';

const Channel = () => {
    return (
        <Container>
            <Header></Header>
        </Container>
    );
};

export default Channel;